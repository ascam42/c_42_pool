int	ft_str_is_alpha(char *str)
{
  int	i;

  i = 0;
  if (!str)
    return (1);
  while (str[i] != '\0')
    {
      if (!(str[i] <= 32 || str[i] == 127))
	    return (0);
      ++i;
    }
  return (1);
}
